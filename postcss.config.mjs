/** @type {import('postcss').Config} */
const config = {
  // Tailwind and Autoprefixer are the standard PostCSS plugins for Next.js + Tailwind setups
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
